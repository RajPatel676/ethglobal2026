# Sepolia key used by `cre workflow simulate --broadcast` to send the report tx
CRE_ETH_PRIVATE_KEY=0x
# Simulation-only value for the secret declared in secrets.yaml
ACCOUNTING_TOKEN=demo-xero-token-change-me

# Secret NAMES only. Values come from .env during simulation, or from the Vault DON
# (`cre secrets create`) for a real deployment. The accounting OAuth token is never in our backend.
secretsNames:
  ACCOUNTING_TOKEN:
    - ACCOUNTING_TOKEN
